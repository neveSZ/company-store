<% if @orders.any? %>
    <% @orders.each do |order| %>
        <%= link_to "Pedido ##{order.id} - #{order.product.name}", order %>
        <%= number_to_currency order.value %>
        <%= "Status: #{order.status}"%>
    <% end %>
<% else %>
    Você ainda não possui pedidos
<% end %>